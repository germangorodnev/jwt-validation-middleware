displayName: Multi JWT Validation Middleware
type: middleware
import: github.com/germangorodnev/multi-jwt-validation-middleware
summary: 'Verify JWT Token in Auth header, Cookie or Query param, and injects decoded payload in header. Supports multiple configurations.'

testData:
  secret: ThisIsMyVerySecret
